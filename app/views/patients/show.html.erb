<h2> <%= @patient.name %> </h2>
<P>
  Appointments:
  <%= @patient.appointments.each do |appointment| %>
  Patient NAME: <%= link_to appointment.patient.name, patient_path(appointment.patient) %> <br> <br>
    <%= link_to appointment.patient.doctor.name, doctor_path(appointment.patient) %>
  <% end %>
